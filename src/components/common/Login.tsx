import { ViewportProvider } from "@Utils/Viewport";
import Detector from "@Utils/Detector";
const Login = () => {
  return (
    <ViewportProvider>
      <Detector />
    </ViewportProvider>
  );
};

export default Login;
